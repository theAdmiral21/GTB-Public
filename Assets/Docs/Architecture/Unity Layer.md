# Unity
The `Unity` layer is the outermost layer of the player controller. It implements Unity types like `MonoBehaviour`, `Input System`, `Animator`, `ScriptableObject` or `Scene`s. Its main responsibility is to translate Unity events into application calls and listen to application events to produce changes in Unity.
