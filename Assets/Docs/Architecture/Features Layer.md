# Feature
The `Feature` layer is above the `Application` layer and contains the concrete implementations of gameplay objects. Things like players, enemies, hazards, and pick ups. The `Feature` layer implements interfaces defined in the `Application` and `Core` layers. Finally, the `Feature` layer is *mostly* game engine agnostic.
